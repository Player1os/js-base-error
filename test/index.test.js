test('Placeholder', () => {
	/*
	let x = 42
	if (false) {
		x = -1
	}

	interface IQ {
		z?: number
	}

	const z: IQ = {}

	console.log(z, Promise.promisify)

	import BaseError from '.../../Personal/node-js-base-error'

	const b = new BaseError('alas' + x)
	throw b
	*/
})
